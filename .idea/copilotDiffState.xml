<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/Main (snapshot).java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/Main (snapshot).java" />
              <option name="originalContent" value="package edu.pucmm.icc352;&#10;&#10;import edu.pucmm.icc352.config.HibernateConfig;&#10;import edu.pucmm.icc352.controllers.*;&#10;import edu.pucmm.icc352.utils.SeedData;&#10;import io.javalin.Javalin;&#10;&#10;public class Main {&#10;&#10;    // Java 25: 'public' en main es redundante, pero no rompe nada&#10;    static void main(String[] unused) {&#10;&#10;        // 1. Inicializar Hibernate&#10;        HibernateConfig.init();&#10;&#10;        // 2. Crear admin por defecto&#10;        SeedData.createDefaultAdmin();&#10;&#10;        // 3. Crear app Javalin 7&#10;        // En Javalin 7: config.routes es un campo, no un metodo&#10;        // Los exception handlers van encadenados despues del create(), antes del start()&#10;        Javalin app = Javalin.create(config -&gt; {&#10;            config.staticFiles.add(&quot;/public&quot;);&#10;            config.bundledPlugins.enableCors(cors -&gt;&#10;                    cors.addRule(it -&gt; it.anyHost())&#10;            );&#10;            // ✅ Javalin 7: config.router.apiBuilder() para registrar rutas&#10;            config.router.apiBuilder(() -&gt; {&#10;                AuthController.register();&#10;                EventController.register();&#10;                RegistrationController.register();&#10;                AdminController.register();&#10;                StatsController.register();&#10;            });&#10;        }).exception(IllegalArgumentException.class, (e, ctx) -&gt; {&#10;            ctx.status(400);&#10;            ctx.json(java.util.Map.of(&quot;error&quot;, e.getMessage()));&#10;        }).exception(IllegalStateException.class, (e, ctx) -&gt; {&#10;            ctx.status(409);&#10;            ctx.json(java.util.Map.of(&quot;error&quot;, e.getMessage()));&#10;        }).exception(SecurityException.class, (e, ctx) -&gt; {&#10;            ctx.status(403);&#10;            ctx.json(java.util.Map.of(&quot;error&quot;, e.getMessage()));&#10;        }).exception(Exception.class, (e, ctx) -&gt; {&#10;            ctx.status(500);&#10;            ctx.json(java.util.Map.of(&quot;error&quot;, &quot;Error interno: &quot; + e.getMessage()));&#10;        });&#10;&#10;        // 5. Arrancar&#10;        app.start(8080);&#10;&#10;        System.out.println(&quot;Servidor corriendo en http://localhost:8080&quot;);&#10;    }&#10;}" />
              <option name="updatedContent" value="package edu.pucmm.icc352;&#10;&#10;import edu.pucmm.icc352.config.HibernateConfig;&#10;import edu.pucmm.icc352.controllers.*;&#10;import edu.pucmm.icc352.utils.SeedData;&#10;import io.javalin.Javalin;&#10;import io.javalin.apibuilder.ApiBuilder;&#10;import java.util.Map;&#10;&#10;public class Main {&#10;&#10;    // Java 25: 'public' en main es redundante, pero no rompe nada&#10;    static void main(String[] unused) {&#10;&#10;        // 1. Inicializar Hibernate&#10;        HibernateConfig.init();&#10;&#10;        // 2. Crear admin por defecto&#10;        SeedData.createDefaultAdmin();&#10;&#10;        // 3. Crear app Javalin 7&#10;        Javalin app = Javalin.create(config -&gt; {&#10;            config.staticFiles.add(&quot;/public&quot;);&#10;            config.bundledPlugins.enableCors(cors -&gt;&#10;                    cors.addRule(it -&gt; it.anyHost())&#10;            );&#10;        });&#10;&#10;        // Registrar rutas usando ApiBuilder&#10;        app.routes(() -&gt; {&#10;            AuthController.register();&#10;            EventController.register();&#10;            RegistrationController.register();&#10;            AdminController.register();&#10;            StatsController.register();&#10;        });&#10;&#10;        // Registrar exception handlers&#10;        app.exception(IllegalArgumentException.class, (e, ctx) -&gt; {&#10;            ctx.status(400).json(Map.of(&quot;error&quot;, e.getMessage()));&#10;        });&#10;&#10;        app.exception(IllegalStateException.class, (e, ctx) -&gt; {&#10;            ctx.status(409).json(Map.of(&quot;error&quot;, e.getMessage()));&#10;        });&#10;&#10;        app.exception(SecurityException.class, (e, ctx) -&gt; {&#10;            ctx.status(403).json(Map.of(&quot;error&quot;, e.getMessage()));&#10;        });&#10;&#10;        app.exception(Exception.class, (e, ctx) -&gt; {&#10;            ctx.status(500).json(Map.of(&quot;error&quot;, &quot;Error interno: &quot; + e.getMessage()));&#10;        });&#10;&#10;        // 4. Arrancar&#10;        app.start(8080);&#10;&#10;        System.out.println(&quot;Servidor corriendo en http://localhost:8080&quot;);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>